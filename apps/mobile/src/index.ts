/**
 * @bta/mobile — entry
 * Exports MobileApp for Android/iOS via React Native.
 * Build target: React Native (Expo) + shared design system.
 */

export { MobileApp, APP_NAME, API_BASE_URL } from "./App";
export { login, listWorkspaces } from "./api/client";
