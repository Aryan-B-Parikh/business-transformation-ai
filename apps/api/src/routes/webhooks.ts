import {Router,Response} from "express";
import {AuthedRequest,authenticate} from "../middleware/auth";
import {authorize} from "../middleware/rbac";
import {getRepositories} from "../repositories";
import {validateWebhookUrl} from "../services/webhook/ssrfGuard";
const router=Router();
router.post("/workspaces/:id/webhooks",authenticate,authorize("org_admin","workspace_admin"),async(req:AuthedRequest,res:Response)=>{const orgId=req.user!.orgId,workspaceId=String(req.params.id),ws=await getRepositories().projects.findWorkspaceById(orgId,workspaceId);if(!ws)return res.status(404).json({error:{code:"NOT_FOUND",message:"Workspace not found"}});const{url,events,secret}=req.body||{};if(typeof url!=="string"||!/^https?:\/\//i.test(url))return res.status(400).json({error:{code:"BAD_REQUEST",message:"valid http(s) url required"}});try{await validateWebhookUrl(url);}catch(e){return res.status(400).json({error:{code:"SSRF_BLOCKED",message:(e as Error).message}});}const cfg=await getRepositories().webhooks.createConfig(orgId,workspaceId,{url,events:Array.isArray(events)&&events.length?events:["artifact.approved","artifact.created"],secret});return res.status(201).json(cfg);});
router.get("/workspaces/:id/webhooks",authenticate,authorize("org_admin","workspace_admin","contributor","reviewer","viewer"),async(req:AuthedRequest,res:Response)=>{const orgId=req.user!.orgId,id=String(req.params.id);if(!await getRepositories().projects.findWorkspaceById(orgId,id))return res.status(404).json({error:{code:"NOT_FOUND",message:"Workspace not found"}});return res.json({data:await getRepositories().webhooks.listConfigs(orgId,id)});});
router.post("/workspaces/:id/webhooks/:webhookId/trigger",authenticate,authorize("org_admin","workspace_admin"),async(req:AuthedRequest,res:Response)=>{const orgId=req.user!.orgId,id=String(req.params.id),cfg=await getRepositories().webhooks.findConfigById(orgId,String(req.params.webhookId));if(!cfg||cfg.workspaceId!==id)return res.status(404).json({error:{code:"NOT_FOUND",message:"Webhook not found"}});const event=typeof req.body?.event==="string"?req.body.event:"artifact.approved";const out=await getRepositories().webhooks.queueOutboxEvent(orgId,event,cfg.id,req.body?.payload||{test:true});return res.status(202).json({queued:out});});
export default router;
